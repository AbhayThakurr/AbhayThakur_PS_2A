#include <iostream>
#include <cstring>
using namespace std;
int main()
{

    char s[100], temp;
    int i, n, count = 0, j;
    cout << "Enter the string \n";
    gets(s);
    n = strlen(s);

    cout << endl;
    for (i = 0; i < n; i++)
    {

        {
            temp = s[n - 1];
        }
    }
    cout << "The suffix is " << temp;

    return 0;
}