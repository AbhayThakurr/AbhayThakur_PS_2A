#include <iostream>
#include <cstring>
#include <string.h>
using namespace std;
int main()
{
    char str[100];
    cout << "Enter the string \n";
    cin >> str;
    strrev(str);
    n = s.length();
    cout << "Reverse of an array is " << str;
    return 0;
}